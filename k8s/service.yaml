apiVersion: v1
kind: Service
metadata:
  name: ansible-agent
  namespace: ansible-system
  labels:
    app: ansible-agent
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: ansible-agent